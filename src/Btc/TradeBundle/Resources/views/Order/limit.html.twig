{% extends 'BtcTradeBundle::layout.html.twig' %}
{% trans_default_domain 'Trade' %}
{% set page_title = 'title.limit_order'|trans() %}

{% block content %}
    <div class="page-header">
        <div class="row row-sm">
            <div class="col-sm-6">
                <h1>{{ page_title }}</h1>
            </div>
        </div>
    </div>
    <div class="row">
      {% include('BtcAccountBundle:widgets:flash_messages.html.twig') %}
    </div>
    {{ include('BtcTradeBundle:Order/forms:limit_deal.html.twig') }}
{% endblock %}
